spring:
  datasource:
    url: jdbc:tc:mysql:5.7:///?TC_INITSCRIPT=schema-mysql.sql
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true